<p id="notice"><%= notice %></p>

<h1>Bonos</h1>

<table>
  <thead>
    <tr>
      <th>Usuario</th>
      <th>Bonos</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_stocks.each do |user_stock| %>
      <tr>
        <td><%= user_stock.user_id %></td>
        <td><%= user_stock.stock_id %></td>
        <td><%= link_to 'Mostrar', user_stock %></td>
        <td><%= link_to 'Editar', edit_user_stock_path(user_stock) %></td>
        <td><%= link_to 'Eliminar', user_stock, method: :delete, data: { confirm: 'Estás seguro/a?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Bono de Usuario', new_user_stock_path %>
